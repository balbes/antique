# global stuff
export
INFO_ORIGIN=ALT Linux Team
INFO_PUBLISHER=ALT Linux
INFO_DISTRIBUTION=ALT Linux

INFO_ARCH=@ARCH@
TARGET=@ARCH@
INFO_LABEL=@LABEL@

INFO_VERSION=@VERSION@

# FIXME: INFO_* won't fall through to profiles/*/Makefile
GLOBAL_RELEASE=$(INFO_RELEASE)

GLOBAL_BOOT_LANG=@LOCALE@
GLOBAL_HSH_APT_CONFIG=@APTCONF@

GLOBAL_TOPDIR=@TOPDIR@
GLOBAL_WORKROOT=$(TMP)

DATE=$(shell date +%Y%m%d)

# byte counts; might be wrong (m4rk3ting 700Mb, 4.7Gb)
CD_SIZE  = 734003200
DVD_SIZE = 4700000000

SQUASHFS_VERSION=@SQUASHFS_VERSION@
